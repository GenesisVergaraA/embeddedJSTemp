<!DOCTYPE html>
<html lang="es" class="productos">
<head>
    <meta charset="UTF-8">
    <title>Editar Producto</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/products.css">
</head>
<body class="productos">
    <header class="productos">
        <h1>Editar Producto: <%= product.nom %></h1>
    </header>
    <main>
        <section class="form-editar-producto">
            <form action="/products/<%= product.id %>?_method=PUT" method="POST">
                <div class="form-group productos">
                    <label for="nom">Nombre:</label>
                    <input type="text" id="nom" name="nom" value="<%= product.nom %>" required>
                </div>
                <div class="form-group productos">
                    <label for="categoria">Categoría:</label>
                    <input type="text" id="categoria" name="categoria" value="<%= product.categoria %>" required>
                </div>
                <div class="form-group productos">
                    <label for="preu">Precio (€):</label>
                    <input type="number" id="preu" name="preu" step="0.01" value="<%= product.preu %>" required>
                </div>
                <div class="actions">
                    <button type="submit" class="btn btn-primary productos">Guardar</button>
                    <a href="/products" class="btn btn-secondary productos">Cancelar</a>
                </div>
            </form>
        </section>
    </main>
</body>
</html>