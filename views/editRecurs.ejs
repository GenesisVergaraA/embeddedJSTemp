<!DOCTYPE html>
<html lang="es" class="recursos">
<head>
    <meta charset="UTF-8">
    <title>Editar Recurso</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/recursos.css">
</head>
<body class="recursos">
    <header class="recursos-header">
        <h1>Editar Recurso: <%= recurs.id %></h1>
    </header>
    <main class="recursos-main">
        <section class="form-editar-recurso">
            <form action="/recursos/<%= recurs.id %>?_method=PUT" method="POST">
                <div class="form-group recursos">
                    <label for="tipo">Tipo:</label>
                    <input type="text" id="tipo" name="tipo" value="<%= recurs.tipo %>" required>
                </div>
                <div class="form-group recursos">
                    <label for="estado">Estado:</label>
                    <select id="estado" name="estado" required>
                        <option value="Disponible" <%= recurs.estado === 'Disponible' ? 'selected' : '' %>>Disponible</option>
                        <option value="Ocupado" <%= recurs.estado === 'Ocupado' ? 'selected' : '' %>>Ocupado</option>
                        <option value="Mantenimiento" <%= recurs.estado === 'Mantenimiento' ? 'selected' : '' %>>Mantenimiento</option>
                    </select>
                </div>
                <div class="recurso-links">
                    <button type="submit" class="btn-editar">Guardar</button>
                    <a href="/recursos" class="btn-detalle">Cancelar</a>
                </div>
            </form>
        </section>
    </main>
</body>
</html>